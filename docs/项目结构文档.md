# 项目结构文档

```bash
code/ # 用于存放主代码
data/ # 用于存放数据集 （已放入.gitignore所以不会上传到github）
    DATA_01/ # 数据集1
    #注意！！这里将解压的TZBDATA文件夹更名为DATA_1 !
    DATA_02/
my_test/ # 用于存放个人的测试脚本 (已放入.gitignore所以不会上传到github)
test/ # 用于存放测试脚本
docs/ # 用于存放项目文档
.gitignore # 用于告诉git仓库哪些文件要被忽略
README.md # 项目说明书
```


